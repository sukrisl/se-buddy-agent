# SE Buddy - pinned transitive dependencies of capellambse==0.8.1.
#
# capellambse itself is installed from vendor/py-capellambse (spec Sec.5.1,
# Sec.7.1), never from this file or from an index. This file exists only so
# that everything capellambse pulls in underneath it is pinned too, per
# Sec.7.1: "Pinning capellambse alone leaves lxml and the rest floating."
#
# Regenerated by installing capellambse==0.8.1 into a clean venv and running
# `pip freeze`, then removing the capellambse line. Only touch this file as
# part of the authorised, recorded pin change described in Sec.7.1 - it moves
# in the same commit as pyproject.toml and the submodule pointer, never alone.
awesomeversion==25.8.0
cached-property==2.0.1
certifi==2026.7.22
charset-normalizer==3.5.1
diskcache==5.6.3
idna==3.19
lxml==6.1.2
MarkupSafe==3.0.3
pillow==12.3.0
platformdirs==4.11.5
python-datauri==3.0.2
PyYAML==6.0.3
requests==2.34.2
svgwrite==1.4.3
typing_extensions==4.16.0
urllib3==2.7.0
